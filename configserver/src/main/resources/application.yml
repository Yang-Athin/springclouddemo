server:
  port: 8762
spring:
  application:
    name: configserver
  profiles:
    #从本地读取
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared